//Calcule a soma dos números de 1 a 60 usando um loop `for`.

let inicio=1,fim=60,soma=0;

for (let index = inicio; index <= fim; index++) {
    soma= soma + index;
}

console.log("A soma dos valores de 1 a 60 é: "+soma);
